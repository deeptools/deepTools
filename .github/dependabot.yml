version: 2
updates:
    - package-ecosystem: github-actions
      directory: /
      schedule:
          interval: weekly
      groups:
          dependencies:
              patterns:
                  - "*"
      commit-message:
          prefix: chore
          include: scope

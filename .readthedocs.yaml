version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
    rust: "1.82"
  apt_packages:
    - clang

sphinx:
  configuration: docs/conf.py

python:
  install:
  - method: pip
    path: .
  - requirements: docs/requirements.txt

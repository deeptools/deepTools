version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.14"
    rust: "1.86"
  apt_packages:
    - clang

sphinx:
  configuration: docs/conf.py

python:
  install:
  - method: pip
    path: .
    extra_requirements:
      - docs
